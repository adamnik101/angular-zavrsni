import { FormatDateFromNowPipe } from './format-date-from-now.pipe';

describe('FormatDateFromNowPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateFromNowPipe();
    expect(pipe).toBeTruthy();
  });
});
