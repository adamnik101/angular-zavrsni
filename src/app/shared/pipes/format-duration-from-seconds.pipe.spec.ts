import { FormatDurationFromSecondsPipe } from './format-duration-from-seconds.pipe';

describe('FormatDurationFromSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDurationFromSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
